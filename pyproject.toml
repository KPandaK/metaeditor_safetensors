[tool.poetry]
name = "metaeditor_safetensors"
version = "0.1.0"
description = "A project for editing safetensors."
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"